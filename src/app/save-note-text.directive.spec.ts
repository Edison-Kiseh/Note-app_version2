import { SaveNoteTextDirective } from './save-note-text.directive';

describe('SaveNoteTextDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveNoteTextDirective();
    expect(directive).toBeTruthy();
  });
});
